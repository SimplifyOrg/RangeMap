# RangeMap
std::map wrapper to store same value for a range of keys. Normal std::map becomes a little inefficient in storing range of key mapped to same value as it stores separate entry for every key. Range map here does not store multiple values but just one to signify the range.
